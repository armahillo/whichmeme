<div id="flash">
<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) && ["error", "notice", "success", "agree", "disagree"].include?(name) %>
    <%= content_tag :div, msg, :class => "flash_#{name}" %>
  <% end %>
<% end %>
</div>